scikit-learn==1.6.1
scikit-image==0.25.2
scipy==1.15.3
soundfile==0.13.1
soxr==0.5.0.post1
librosa==0.11.0
# libsndfile1 是系统库（Debian/Ubuntu: `apt-get install -y libsndfile1`），不是 pip 包。
# pip 无法提供 soundfile/rubberband 所需的 libsndfile 运行时库，必须用系统包管理器安装。
sympy==1.13.1
audio-separator==0.30.2
pyloudnorm==0.1.1
nvidia-ml-py==13.580.65
tzdata==2025.2
onnx==1.18.0
# GPU 版 ORT 同时包含 CPU fallback；不要与 `onnxruntime` CPU 包同时安装。
# 1.26.x 使用 CUDA 12.x + cuDNN 9，与本项目 PyTorch 2.6.0+cu124 匹配；
# 1.27+ 默认已转向 CUDA 13，因此本 CUDA 12 部署固定在 1.26.x。
onnxruntime-gpu==1.26.0
# tritonserverclient 是 NVIDIA Triton 推理服务客户端，PyPI 无此包名/版本，
# 且本仓库本地数字人生成不依赖 Triton 服务，故移除。
openai==1.75.0
numpy==1.26.4
cffi==2.0.0
chardet==5.2.0